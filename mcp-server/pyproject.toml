[project]
name = "taskflow-mcp"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.28.1",
    "mcp[cli]>=1.28.1",
]

[dependency-groups]
dev = [
    "ruff>=0.15.21",
]
